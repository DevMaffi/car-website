include ../base/vars
include ../base/mixins

doctype html
html(lang="en")
  block head
    +head()

  // BODY
  body

    // MAIN
    main.main
      block header 

      block content

      block footer

    block scrollUp

    // svg element with blur filter applied
    svg.hide-svg#svg-image-blur
      filter#blur-effect
        feGaussianBlur(stdDeviation="86")
